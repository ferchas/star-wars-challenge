version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.14.2
    steps:
      - checkout
      - run: echo "Build app"
  lint:
    docker:
      - image: circleci/node:10.14.2
    steps:
      - checkout
      - run:
          name: install-npm
          command: npm install
      - run:
          name: Eslint
          command: npm run lint
      - run:
          name: test no past
          command: echo "Nope!"; exit 1

workflows:
  version: 2
  build_and_lint:
    jobs:
      - build
      - lint